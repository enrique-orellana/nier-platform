import React from 'react';
import { ChevronDown, ChevronRight } from 'lucide-react';

export default function TrackControls({ track, onChange, isCollapsed, onToggleCollapse }) {
    return <div className="sticky left-0 z-10 flex w-40 shrink-0 items-center gap-1 border-r border-white/10 bg-[#17171b] px-2 text-[10px] text-zinc-300"><button type="button" onClick={onToggleCollapse} className="text-zinc-500 hover:text-white shrink-0">{isCollapsed ? <ChevronRight size={14} /> : <ChevronDown size={14} />}</button><span className="font-mono text-cyan-300 flex-1 truncate">{track.name}</span><button type="button" aria-label={`${track.name} mute`} onClick={() => onChange({ ...track, muted: !track.muted })} className={track.muted ? 'text-red-300 shrink-0' : 'text-zinc-500 hover:text-zinc-300 shrink-0'}>M</button><button type="button" aria-label={`${track.name} lock`} onClick={() => onChange({ ...track, locked: !track.locked })} className={track.locked ? 'text-amber-300 shrink-0' : 'text-zinc-500 hover:text-zinc-300 shrink-0'}>L</button><button type="button" aria-label={`${track.name} visibility`} onClick={() => onChange({ ...track, visible: track.visible === false })} className={track.visible === false ? 'text-zinc-700 shrink-0' : 'text-zinc-500 hover:text-zinc-300 shrink-0'}>◉</button></div>;
}
